# Codex discovery metadata for the dotnet-artisan plugin.
# Read by $skill-installer when installing from novotnyllc/dotnet-marketplace.

interface:
  display_name: "dotnet-artisan"
  short_description: "130 .NET skills for modern C# and cloud apps"
  default_prompt: >
    $skill-installer novotnyllc/dotnet-marketplace — install .NET development
    skills. All 130 skills are under plugins/dotnet-artisan/skills/<category>/<skill-name>/,
    each containing a SKILL.md file.

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Library documentation lookup for .NET frameworks"
      transport: "streamable_http"
      url: "https://mcp.context7.com/mcp"
    - type: "mcp"
      value: "microsoftdocs"
      description: "Official Microsoft Learn documentation search"
      transport: "streamable_http"
      url: "https://learn.microsoft.com/api/mcp"
